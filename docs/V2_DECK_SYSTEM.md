# Word Shorts 2.0 - 덱 시스템 PRD

> **현재 브랜치**: `phase-2`
> **시작일**: 2026-03-01

---

## 📋 개요

Git의 staging/commit 컨셉을 차용한 단어 암기 단계 관리 시스템

---

## 🎯 핵심 개념

### 덱 (Deck) 구조

```
┌─────────────────────────────────────────────────────────┐
│                        덱 (Deck)                        │
├─────────────────────────────────────────────────────────┤
│  📦 원본 배열 (immutable)                               │
│     └── 영단어 원본 데이터, 절대 수정 안 됨              │
│                                                         │
│  📋 미암기 배열                                         │
│     └── 초기에 원본 클론, 암기 시작 전 단어들            │
│                                                         │
│  🔄 암기 중 배열                                        │
│     └── 현재 학습 중인 단어들                           │
│                                                         │
│  ✅ 암기 완료 배열                                      │
│     └── 완전히 암기한 단어들                            │
└─────────────────────────────────────────────────────────┘
```

### 단어 이동 흐름

```
미암기 ──────────────────────────────────► 암기 중
   │                                          │
   │                                          │
   │                                          ▼
   │                                      암기 완료
   │                                          │
   │          ◄────────────────────────────────
   │          (다시 학습 필요시)
   ▼
[Git 비유: Working Dir → Staging → Committed]
```

---

## 🧭 네비게이션 구조

### 하단 네비게이션 (3탭)

```
┌─────────────────────────────────────────────────────────┐
│   📦 Deck      │   📱 Shorts     │   ✓ Checklist      │
│   (Style)      │   (Video)       │   (PlaylistAddCheck)│
│   /deck        │   /             │   /checklist        │
└─────────────────────────────────────────────────────────┘
```

### 아이콘 매핑
| 페이지 | 경로 | MUI 아이콘 | 설명 |
|--------|------|------------|------|
| Deck | `/deck` | `Style` | 덱 선택 + 학습 단계 선택 |
| Shorts | `/` | `OndemandVideo` | TikTok 스타일 학습 |
| Checklist | `/checklist` | `PlaylistAddCheck` | 전체 단어 목록 + 검색/필터 |

---

## 📱 페이지 구조

### 1. 덱 뷰 페이지 (`/deck`)

```
┌─────────────────────────────────────────────────────────┐
│  📚 기본 영단어                                         │
│  Word Shorts 기본 단어장                                │
├─────────────────────────────────────────────────────────┤
│  전체 진행률                           45 / 124 (36%)   │
│  ████████████░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░  │
│                                                         │
│  [미암기 79] [암기중 0] [완료 45]                        │
├─────────────────────────────────────────────────────────┤
│  학습할 단계 선택                                       │
│                                                         │
│  ┌─────────────────────────────────────────────────┐   │
│  │ ❓ 미암기                              79개 (64%) │   │
│  └─────────────────────────────────────────────────┘   │
│  ┌─────────────────────────────────────────────────┐   │
│  │ 📖 암기중                               0개 (0%)  │   │
│  └─────────────────────────────────────────────────┘   │
│  ┌─────────────────────────────────────────────────┐   │
│  │ ✅ 완료                               45개 (36%) │   │
│  └─────────────────────────────────────────────────┘   │
├─────────────────────────────────────────────────────────┤
│  ┌─────────────────────────────────────────────────┐   │
│  │              ▶ 학습 시작 (79개)                  │   │
│  └─────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────┘
```

### 2. 쇼츠 뷰 페이지 (`/` - 메인)

```
┌─────────────────────────────────────────────────────────┐
│                                                         │
│                    [이미지]                             │
│                                                         │
│                   "abandon"                             │
│                   버리다                                │
│                                                         │
│     [🔊]    [👁️]    [📖]    [✅ 암기]                  │
│                                   ↑                     │
│                        미암기→암기중 / 암기중→완료       │
└─────────────────────────────────────────────────────────┘
```

### 3. 체크리스트 페이지 (`/checklist`)

```
┌─────────────────────────────────────────────────────────┐
│  🔍 단어 검색...                                        │
├─────────────────────────────────────────────────────────┤
│  [전체 124] [미암기 79] [암기중 0] [완료 45]  ← 필터    │
├─────────────────────────────────────────────────────────┤
│  #  │ 단어        │ 뜻           │ 상태                 │
├─────────────────────────────────────────────────────────┤
│  1  │ abandon     │ 버리다       │ [완료]               │
│  2  │ ability     │ 능력         │ [미암기]             │
│  3  │ abolish     │ 폐지하다     │ [미암기]             │
│  4  │ abroad      │ 해외에       │ [완료]               │
│  ...                                                    │
└─────────────────────────────────────────────────────────┘
         ↑ 가상화 스크롤 (@tanstack/react-virtual)
```

**기능:**
- 🔍 검색 (단어, 뜻)
- 🏷️ 필터 (전체/미암기/암기중/완료)
- 📊 상태 배지 표시
- ⚡ 가상화 스크롤 (대용량 단어 목록 대응)

---

## 🛠️ 기술 스택

| 분야 | 선택 | 이유 |
|------|------|------|
| **상태 관리** | Zustand | 간단, 보일러플레이트 적음 |
| **로컬 저장소** | Dexie.js | IndexedDB 래퍼, 타입 지원 우수 |
| **가상화** | @tanstack/react-virtual | 최신, 경량, 유연 |
| **UI** | MUI | 기존 유지 |

---

## 📊 데이터 구조

### TypeScript 타입

```typescript
// 단어 타입
interface Word {
  id: string;
  word: string;        // 영단어
  slug: string;        // URL용 슬러그
  meaning_en: string;  // 영어 뜻
  meaning_kr: string;  // 한국어 뜻
}

// 암기 단계
type MemorizationStage = 'unlearned' | 'learning' | 'mastered';

// 덱 타입
interface Deck {
  id: string;
  name: string;              // 덱 이름
  description?: string;      // 설명
  words: Word[];             // 원본 단어 배열 (immutable)
  unlearned: string[];       // 미암기 단어 ID 배열
  learning: string[];        // 암기 중 단어 ID 배열
  mastered: string[];        // 암기 완료 단어 ID 배열
  createdAt: number;
  updatedAt: number;
}

// 덱 통계
interface DeckStats {
  total: number;
  unlearned: number;
  learning: number;
  mastered: number;
  unlearnedPercent: number;
  learningPercent: number;
  masteredPercent: number;
}
```

---

## 📝 개발 태스크

### Phase 1: 기반 작업 ✅ 완료

- [x] IndexedDB 설정 (Dexie) - `src/db/index.ts`
- [x] Deck 타입 정의 - `src/types/deck.ts`
- [x] Zustand 스토어 - `src/store/deckStore.ts`
- [x] 네비게이션 변경 (Quiz → Checklist)

### Phase 2: 덱 뷰 페이지 ✅ 완료

- [x] 덱 이름 + 설명 표시
- [x] 전체 진행률 바
- [x] 암기 단계 선택 카드 UI
- [x] 쇼츠 시작 버튼
- [x] 가독성/스타일 개선

### Phase 3: 체크리스트 페이지 ✅ 완료

- [x] 검색 기능 (단어, 뜻)
- [x] 단계별 필터 (전체/미암기/암기중/완료)
- [x] Virtualized 리스트 구현
- [x] 상태 배지
- [x] 알파벳순 정렬

### Phase 4: 쇼츠 뷰 업데이트 ✅ 완료

- [x] 현재 선택된 단계의 단어만 표시
- [x] 암기 버튼 추가 (미암기→암기중, 암기중→완료)
- [x] 덱 스토어 연동
- [x] 상단에 현재 스테이지 표시
- [x] 빈 상태 처리

### Phase 5: 마무리

- [ ] AppBar 업데이트 (현재 덱/단계 표시)
- [ ] 에러 핸들링
- [ ] 테스트

---

## 🗂️ 파일 구조

```
src/
├── db/
│   └── index.ts           # Dexie IndexedDB 설정
├── store/
│   └── deckStore.ts       # Zustand 덱 스토어
├── types/
│   └── deck.ts            # 타입 정의
├── pages/
│   ├── Deck.tsx           # 덱 선택 페이지
│   ├── Shorts.tsx         # 쇼츠 학습 페이지
│   └── Checklist.tsx      # 체크리스트 페이지
├── components/
│   └── AppLayout.tsx      # 레이아웃 + 네비게이션
└── App.tsx                # 라우팅 + 초기화
```

---

## ✅ 기술 결정 사항 (확정)

### 1. IndexedDB Wrapper: **Dexie.js** ✅

```bash
npm install dexie
```

### 2. Virtualization: **@tanstack/react-virtual** ✅

```bash
npm install @tanstack/react-virtual
```

### 3. 데이터 전략: **로컬 우선 (Local-First)** ✅

```
앱 시작
  ↓
IndexedDB에 덱 있음?
  ├─ YES → 로컬 데이터 사용 (빠름!)
  └─ NO  → API에서 가져와서 IndexedDB에 저장
```

---

*마지막 업데이트: 2026-03-01*
